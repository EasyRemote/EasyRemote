# Intent

Remove EasyRemote test transport descriptor fixtures that bypass the canonical SDK addressing model.

The fake runtime transport should project runtime ability descriptor references through the same SDK addressing seam used by production code.

